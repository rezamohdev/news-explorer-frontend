import React from 'react';
import './PopupWithForm';

export default function PopupWithForm({ children, buttonText, title, onClose, name, isOpen, onSubmit, }) {
    return (
        <div className='form-popup'>

        </div>
    )
}
